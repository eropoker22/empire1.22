# Server Bootstrap

Composition root for authoritative runtime.

- Wires transport, persistence, projections, mode config, and game core
- Boots `free` and `war` instances from config factories
- Owns source-of-truth runtime processes

