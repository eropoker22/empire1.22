/**
 * Responsibility: Admin module boundary for diagnostics and readiness views.
 * Belongs here: stale, lag, and degraded-state admin UI seams.
 * Does not belong here: scheduler logic or gameplay rules.
 */
export const diagnosticsAdminModule = "diagnostics-admin-module";

