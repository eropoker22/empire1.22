/**
 * Responsibility: Admin module boundary for snapshot overview and restore metadata views.
 * Belongs here: snapshot-focused presentation seams.
 * Does not belong here: snapshot storage or state restoration logic.
 */
export const snapshotsAdminModule = "snapshots-admin-module";

