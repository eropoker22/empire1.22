/**
 * Responsibility: Presentation shell for loading and empty client states.
 * Belongs here: read-only visual fallback boundaries.
 * Does not belong here: transport retries or gameplay logic.
 */
export const renderLoadingState = (): void => {
  return;
};

