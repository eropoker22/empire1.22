/**
 * Responsibility: Isolated debug-only client presentation seam.
 * Belongs here: optional development/debug surface outside the production UI path.
 * Does not belong here: production game shell or authoritative state logic.
 */
export const renderDebugPanel = (): void => {
  return;
};

