/**
 * Responsibility: Shared mode identifiers used across client, server, and config.
 * Belongs here: stable cross-layer string literals and type aliases.
 * Does not belong here: runtime config lookup, branching logic, or UI decisions.
 */
export type GameModeId = "free" | "war";

