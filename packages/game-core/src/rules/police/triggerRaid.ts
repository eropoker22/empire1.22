import type { CoreGameState } from "../../entities";

/**
 * Responsibility: Placeholder for future raid triggering transitions.
 * Belongs here: police-driven state transitions in the core.
 * Does not belong here: transport or UI effects.
 */
export const triggerRaid = (state: CoreGameState): CoreGameState => state;

