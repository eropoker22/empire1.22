import type { CoreError } from "../../errors";

/**
 * Responsibility: Future combat-specific validation helpers.
 * Belongs here: pure precondition checks for combat actions.
 * Does not belong here: transport auth or UI form validation.
 */
export const validateCombatAction = (): CoreError[] => [];

