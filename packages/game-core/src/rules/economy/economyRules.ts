/**
 * Responsibility: Groups reusable economy constants and policy keys.
 * Belongs here: thin shared helpers that support economy rule modules.
 * Does not belong here: mutable game state or command handling.
 */
export const economyRuleSet = "default-economy";

