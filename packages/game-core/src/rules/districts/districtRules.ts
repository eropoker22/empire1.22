/**
 * Responsibility: Holds district rule placeholders and shared policy identifiers.
 * Belongs here: district ownership/build limits policy references.
 * Does not belong here: UI map rendering or transport routing.
 */
export const districtRuleSet = "default-districts";

