import type { CoreError } from "../errors";

/**
 * Responsibility: Placeholder validator for alliance membership changes.
 * Belongs here: alliance-specific precondition checks.
 * Does not belong here: chat, invite transport, or persistence.
 */
export const validateAlliance = (): CoreError[] => [];

